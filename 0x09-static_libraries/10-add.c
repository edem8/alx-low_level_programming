#include <stdio.h>
#include "main.h"

/**
 * add - printing the result from a sum
 * @a: parameter to pass
 * @b: parameters to be passed
 * Return: return (0) or nothing
*/
int add(int a, int b)
{
	return (a + b);
}
